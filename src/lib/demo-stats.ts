// Demo figures — placeholder platform stats shared by /how-it-works and
// /activity so the two pages never disagree; swap for live data when the
// backend stats feed lands.
export const DEMO_STATS = {
  transactions: '1.9M',
  volume: 'RM 167.9M',
  listings: '19.8K',
} as const;
